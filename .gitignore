# Dataset lives on /mnt/ace; data/{raw,interim,processed} are symlinks.
# Match both the directory form and the symlink form by omitting trailing slash.
data/raw
data/interim
data/processed

# Python / uv
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Notebook artifacts
.ipynb_checkpoints/
notebooks/**/*.ipynb_checkpoints/

# Kaggle artifacts
*.zip
submissions/
.kaggle/

# Secrets — never commit
kaggle.json
.env
.env.*

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Agent-context local-only scoping (added 2026-05-07 on nest-into-workspace-hub)
.claude/

# Built static Pages output (regenerated in CI)
public/
